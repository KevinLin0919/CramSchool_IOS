<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
	<!-- Answered here rather than by hand on every upload. The question is
	     about proprietary cryptography, and this app implements none: it makes
	     ordinary network calls and stores a token in the Keychain, all of it
	     the platform's own. Leaving the key out makes App Store Connect ask
	     the same question about every single build before it can reach a
	     tester, which is a manual gate on an otherwise automatic pipeline. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- Set by CI for the QAT build (FUDAO_ENV=qat). Empty everywhere else,
	     which AppEnvironment reads as production. -->
	<key>FudaoEnvironment</key>
	<string>$(FUDAO_ENV)</string>
	<key>NSCameraUsageDescription</key>
	<string>需要使用相機掃描學生考卷，進行即時辨識與批改。</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>需要存取相簿以選取考卷照片進行批改。</string>
</dict>
</plist>
